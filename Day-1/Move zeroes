class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        start = 0
        for num in nums:
            if num != 0:
                nums[start] = num
                start += 1

        while start < size:
            nums[start] = 0
            start += 1
        return nums

            


        
        
